**用中文思考。**

输入：`{target_work_dir}/.vuln_agent_output/discovered_surfaces/` 下的全部攻击面文件。每个文件的完整内容已在下方逐个列出（以 `### <文件名>` 为头）。

你的职责**只做一件事**：对所有攻击面做**全局优先级排序**，决定下游业务流分析的执行顺序（高优先级先分析——中断恢复时更可能已完成）。

## 待排序攻击面

{surfaces_block}

## 排序依据（仅从攻击面文件已有信息做前置分类评估）

- **暴露/可达性**：互联网可达的 REST/Web 入口 > 内部 MQ/gRPC > CLI/脚本/独立工具 > DEPLOY/部署配置。
- **敏感操作信号**：`发现` 字段命中命令执行 / SQL / 文件读写 / 认证 / 反序列化 / 出站请求等高危操作的，优先级更高。
- **输入丰富度**：可被外部输入控制的参数越多、入参链越长，优先级越高。
- **信任边界**：跨信任边界（外网→内网、未认证→特权）的优先级更高。

## 输出

在回答末尾给出 `## PRIORITY_RESULT` 清单，**按优先级从高到低**逐行列出上方每一个攻击面，格式严格如下（每行一个）：

- <文件名>.md | high | <不超过20字的理由>
- <文件名>.md | medium | <理由>
- <文件名>.md | low | <理由>

## 约束
- 必须覆盖上方列出的**全部**攻击面，每个文件恰好出现一次。
- `<文件名>` 用攻击面的真实文件名（含 `.md`），与上方 `### <文件名>` 头一致。
- 优先级档位只用 `high` / `medium` / `low`。
- 理由用中文，简短、基于事实（暴露面+敏感操作），**不下“是漏洞”的结论**。
- **不创建、不修改、不删除任何文件；不读源码。** 只输出排序清单。
