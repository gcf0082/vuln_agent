# 自动忽略的漏洞类别

以下类别命中时直接跳过：

- **JSON 反序列化**：JSON 处理工具（Jackson/Fastjson 等）的反序列化行为由三方库自身决定，业务代码仅做常规调用，不视为业务漏洞
- **日志注入**：影响太小，不关心
- **路径类环境变量**：无直接证据的情况下，所有表示文件或目录路径的环境变量都是安全的，不存在外部控制的风险
- **敏感信息认定**：只有**明文或加密的认证凭据类信息**（密码、Token、密钥、证书私钥、Cookie等）、**电话号码**、**邮箱地址**视为敏感信息。除此以外的信息（如用户名、昵称、ID、地址、非私钥的证书字段等）不认定为敏感信息，不得因此报告漏洞
